# Oracle Instant Client

Place Oracle Instant Client files in `oracle/instantclient` on the Ubuntu server before starting the API container.

The folder is mounted read-only into the API container at `ORACLE_INSTANT_CLIENT_DIR`.

